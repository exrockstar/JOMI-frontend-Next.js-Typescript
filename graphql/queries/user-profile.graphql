query UserProfile {
  user {
    ...UserProfileParts
  }
  userAccessType {
    accessType
    institution_name
    shouldRequestInstVerification
    viaTemporaryIp
    expiry
    subscriptionExpiresAt
    requireLogin
    institution_id
  }
  geolocation {
    countryCode
  }
}
